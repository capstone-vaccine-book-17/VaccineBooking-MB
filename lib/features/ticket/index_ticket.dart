import 'package:flutter/material.dart';

class IndexTicket extends StatelessWidget {
  const IndexTicket({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Ticket'),
      ),
    );
  }
}
